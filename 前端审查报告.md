# Anima Prompt Studio 前端审查报告

日期:2026-07-09 · 范围:`prototype/index.html`(833 行)、`app.js`(2851 行)、`styles.css`(2245 行)、`data.js`(293 行)

## 总评

前端整体质量高于一般原型水平:单向数据流(dispatch/reducer)结构清晰,fetch 均有错误处理并落到 toast,计时器有清理,设置持久化走后端而非 localStorage(符合项目规则),toast 和进度区带 `aria-live`,44 个 Node 测试全部通过。主要问题集中在一处:**HTML 拼接时转义不一致,存在注入风险**。

## 高优先级:innerHTML 转义不一致(注入/破版风险)

代码里已有 `escapeHtml()`(第 1072 行),资源列表 `renderQuickPicks` 用得很规范,但另外几处 innerHTML 直接插值未转义:

| 位置 | 未转义内容 | 数据来源 | 风险 |
|---|---|---|---|
| `renderBlocks`(约 2270 行) | `block.en` / `block.zh` 直接嵌入 `<textarea>...</textarea>` | **LLM 输出**(本地或外部 API) | 最高。输出含 `</textarea>` 即破坏页面;恶意/被劫持的外部 API 可注入脚本 |
| `renderAnalyzers`(约 2010 行) | `model.error`、`model.detail` | 后端错误文本(可能包含异常原文) | 中 |
| `renderReferences`(1724 行) | `item.title`、`item.promptZh`、`item.image` | 目前是静态 `data.js`,暂时安全;但路线图计划改为用户持久化数据 | 现在低,将来高 |
| `renderBlocks` 折叠视图 | `block.label`、`block.source` | 本地常量 + 服务端 | 低 |

修复建议:所有插入 HTML 的动态值一律过 `escapeHtml`;textarea 更稳妥的做法是模板里留空,渲染后统一 `element.value = block.en` 赋值(顺带避免转义遗漏)。这是机械性改动,半小时内可完成,建议同时补一条"LLM 输出含 HTML 特殊字符"的 Node 测试用例。

## 中优先级

1. **`apiJson` 对非 JSON 错误响应会二次抛错**(1097 行)。`await response.json()` 在后端返回非 JSON(如进程崩溃、代理 502)时抛 `SyntaxError`,用户看到的 toast 变成英文解析错误。建议把 `json()` 包进 try/catch,失败时退回 `HTTP ${status}`。
2. **长请求无法取消**。文本扩写/全随机/图片分析耗时几十秒到几分钟,fetch 没有 AbortController,页面上只能干等(state 防重入做了,但底层请求仍在跑)。路线图已列"可取消进度反馈",建议实现时前后端一起做。
3. **`renderBlocks` 两套几乎相同的模板重复约 100 行**(折叠/展开视图,2146-2273 行)。差异只有中间一段,提取成 `blockHead(block)` + 条件插入 body 即可,能显著降低后续改动漏改一边的风险。
4. **手动 cache-busting 版本号**。`?v=20260622-model-random-v3` 在 index.html 出现 3 处,靠手改容易漏。本地单机场景更简单的办法是后端对静态资源返回 `Cache-Control: no-cache`,把版本号整个去掉。

## 低优先级

5. **app.js 单文件 2851 行**。dispatch 模式本身清晰,但文本生成、图片分析、资源面板、设置四个域挤在一个闭包里,建议按视图拆原生 ES Module(无需构建工具)。
6. **无响应式**。styles.css 没有任何 `@media`,按 1920×1080 设计。本地工具可接受,但 1366/1440 宽笔记本会出横向滚动,可加一档窄屏断点。
7. **`data.js` 静态参考条目与路线图的"参考提示词库"重叠**。持久化版本落地后记得删掉静态数据和对应渲染分支,避免两套来源并存。

## 值得保留的好做法

块内文本编辑(`UPDATE_BLOCK`)刻意绕过全量重渲染、只改 state 和 dirty 标记,避免了 textarea 光标丢失——这个细节处理得好。事件全部走 document 级委托,重渲染不会丢监听;权重滑条即时更新只动局部 DOM。整体架构不需要推翻,按上面顺序修即可。

## 建议动作顺序

1. 修转义(高优先级表格 4 处)+ 补注入测试
2. 修 `apiJson` 的 JSON 解析容错
3. 合并 `renderBlocks` 重复模板
4. 其余按需排入路线图
