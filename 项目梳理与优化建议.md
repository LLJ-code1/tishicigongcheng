# 项目梳理与优化建议

日期:2026-07-09 · 仓库:LLJ-code1/tishicigongcheng

## 总体结论

项目结构清晰、文档齐全、测试覆盖好,但 **GitHub 上的仓库不完整**:一个核心源文件因 `.gitignore` 写法问题从未被提交,导致新克隆的仓库跑不起来(87 个 Python 测试中 10 个失败)。本次已在本地修复并验证,你需要提交推送一次。

## 严重问题(已修复,待你推送)

**`prototype/prompts/text/models/anima.md` 缺失。**

- 根因:`.gitignore` 第 21 行写的是 `models/`(不带前导 `/`),它会忽略仓库里**所有**叫 `models` 的目录——不仅是根目录的视觉模型目录,也包括提示词模块目录 `prototype/prompts/text/models/`。
- 影响:`prompt_engine.py` 和 `server.py` 都引用该文件,新克隆后文本扩写、全随机、拆解全部报 `提示词模块无法读取:anima.md`,10 个测试失败。
- 修复(已完成):
  1. `.gitignore` 中 `models/` 改为 `/models/`(只忽略根目录)。
  2. 从编译产物 `prompts/compiled/text_expand_anima.md` 中逐字节还原了 `anima.md`——`test_compiled_review_file_matches_runtime_system_prompt` 通过,证明还原内容与你本机原文完全一致。
- **你需要做的**:在本机原始工作目录中同样修改 `.gitignore`,然后
  ```powershell
  git add .gitignore prototype/prompts/text/models/anima.md
  git commit -m "fix: 提交被 gitignore 误排除的 anima.md 提示词模块"
  git push
  ```

## 验证结果(Linux 环境)

| 测试 | 结果 |
|---|---|
| prototype/tests(Python,87 个) | 修复前 10 失败 → 修复后全部通过 |
| prototype/tests/app.test.js(Node,44 个) | 全部通过 |
| scripts/tests(Python,3 个) | 全部通过 |

安全检查:服务默认绑定 127.0.0.1(可通过环境变量覆盖)✓;全库未扫到硬编码 API Key ✓;数据库、模型、.env 均已正确排除 ✓。

## 其他优化建议(按优先级)

1. **在 CI 里跑"干净克隆"测试**。这次的问题本质是"本机能跑 ≠ 克隆能跑"。加一个 GitHub Actions workflow,每次 push 后在干净环境执行三套测试,可以永久防止此类文件缺失。这也正好落实你 roadmap 里"自动化的文档/API 一致性检查"一项。
2. **`app.js`(2851 行)和 `styles.css`(2245 行)单文件过大**。建议按功能域拆分(作品管理 / 文本生成 / 图片分析 / AnimaDex 面板),用原生 ES Module 即可,不需要引入构建工具,符合本地优先原则。
3. **验证截图移出源码目录**。`prototype/` 根下有 9 张 PNG(约 1.7 MB)是开发验证截图,建议移到 `docs/assets/`,让 `prototype/` 只保留可运行代码。`data/*-validation.json` 同理。
4. **根目录文档归拢**。`MiaoMiao_Harem_Anima_项目规范.md`、`WAI_ANIMA_模型规范.md`、`Anima-Style-Explorer_接入评估.md`、`项目落地评估.md`、`给其他AI的_..._详细说明.md` 建议移入 `docs/`,根目录只留 README/CLAUDE/AGENTS,并在 `docs/README.md` 索引中登记。
5. **给编译产物加一致性守护**。`text_expand_anima.md` 是生成文件,已有测试校验它与运行时一致,很好;建议在 CI 中也跑 `export_text_expand_prompt.py` 后 `git diff --exit-code`,防止改了模块忘了重新导出。
6. **README 补充 Windows 之外的说明**。代码本身跨平台(纯 stdlib + Node),但文档只有 PowerShell 命令;哪怕加一句"非 Windows 用户直接 `python prototype/server.py`"也能降低门槛。

## 项目完整性清单

- 源码 + 测试:✓ 完整(修复 anima.md 后)
- 文档:✓ 完整(架构、数据模型、路线图、开发日志、接入文档齐全,质量高)
- 提示词归档:✓ 完整(manifest + 去重版 + 按来源副本)
- 子模块:⚠ 未验证——AnimaDex 与 Anima-Style-Explorer 两个子模块体积较大,在线环境拉取超时。请在本机确认 `git submodule update --init --recursive` 可正常完成。
- 本机状态(models/、animadex-data/、*.db):按设计不入库,✓ 正确

## 关于同步到本地的说明

仓库已同步到你选择的文件夹(浅克隆,只含最新一次提交的历史),其中已包含修复后的 `.gitignore` 和还原的 `anima.md`。如果你本机已有完整工作目录,建议以本机目录为准,只把上述两处修复应用过去。
