# 开发日志

## 2026-07-17 完整 Recipe、可靠保存、确定性词库与中文编辑闭环

### 完整配方与前端工作台

- 将工作台和文本模型输出从十块迁移为十三块：质量、画师/风格、人物身份、外观、
  服装、表情、动作、互动、场景、镜头、光影、特效和负向约束；用兼容适配器连接
  Recipe 的规范块 ID。
- 新增 Recipe v1，将精确模型引用、双语正负提示词、十三块、LoRA 列表、参数及来源、
  random plan、中文修改历史、图片/来源引用和 metadata 一并规范化并计算 SHA-256。
  每个新版本在 metadata 中保存完整 Recipe 和 Hash，重开不再只恢复提示词文本。
- 新增 Anima_1.1 模型档案，绑定版本 ID `3004063`，默认 Steps 30、CFG 5.5、
  Euler/Euler a + Normal，正向前缀不含 `safe`。checkpoint 文件/Hash 和尺寸没有可靠
  本机证据，因此候选尺寸不冒充验证结果，`generationReady=false`。
- 前端增加完整配方控制台、参数来源/手动覆盖、Seed、中文修改预览和备份入口；修复
  主渲染漏掉配方控制台及 1920×1080 下控制台横向溢出。

### 原子保存与响应丢失恢复

- 新增 `POST /api/workspace/commit`：客户端预分配操作、项目和版本 ID，项目头、可选
  版本和幂等账本在一个 `BEGIN IMMEDIATE` 事务中提交。相同请求重放返回第一次结果，
  不同正文复用键返回 `idempotency_conflict`。
- 前端在发送前把冻结正文写入 `promptStudio.pendingSave.v1`，覆盖“数据库提交成功但 HTTP
  响应丢失”、刷新和启动恢复。账本损坏、引用悬空和写账本失败均失败关闭或整体回滚。
- 并发回归覆盖相同操作 8 路重放只产生一个版本、不同正文只有一个胜者、项目头与版本
  不出现半保存。旧项目/版本端点继续支持独立幂等键和 CAS。

### 确定性词库与中文最小差异编辑

- 新增 `sha256-counter-v1` sampler：128 位十六进制 Seed、类别独立哈希流、无偏拒绝
  采样、锁定、重抽、硬/软冲突和完整轨迹；random plan 不调用文本模型。
- 接入 `GET /api/text/random-catalog`、`POST /api/text/random-plan` 和前端 Seed 入口。
  抽中项应用到十三块并随 Recipe 保存；中文暂标记待本地 LLM 翻译。
- 目录继续保持 `runtimeReady=false`、`semanticReviewRequired=true`；只有
  `PROMPT_STUDIO_EXPERIMENTAL_WORDLISTS=1` 才允许计划抽样。471 条逐项语义审核、不可变
  目录历史和十三块原生 mapping 尚未完成，不把实验链路描述为正式发布。
- 新增中文编辑 preview/apply/undo 三个接口：确定性解析已登记指令，展示受影响/锁定块
  和前后差异；旧 Hash、伪造预览、未知指令及锁定依赖冲突均拒绝。应用和撤销追加
  `instructionHistory` 并自动保存新版本。

### 逻辑备份与隔离恢复

- 新增全库/单项目 JSON/ZIP 逻辑导出，manifest 记录格式版本、文件大小和 SHA-256；
  排除 API Key、接口 URL、内部幂等账本、模型、LoRA、图片和其他二进制资产。
- 新增只读 inspect 和隔离 stage-restore。恢复前拒绝重复 JSON key、Hash 篡改、ZIP
  slip、重复/加密/非普通成员、超限文件、解压总量和压缩炸弹；冲突可选 reject/rename。
- HTTP 恢复始终写 `.prompt-studio-recovery/restore-preview-*.db` 并返回
  `activated=false`，不会替换正在运行的数据库。首页和项目页已接入导出/检查/确认/
  隔离恢复流程。

### 全流程与对抗验证

- 自动化全流程覆盖：固定 Seed → Recipe → V1 原子保存 → 响应丢失重放 → 中文服装修改
  V2 → 撤销 V3 → 全库导出 → 新库恢复 → 精确重开全部历史。
- 真实浏览器完成：词库 Seed 生成十三块、保存 V1、手动 sampler 覆盖并保存 V2、中文
  修改为红色连衣裙保存 V3、撤销保存 V4、刷新重开并核对 Recipe/Seed/参数/四个版本；
  控制台无 error/warning，页面无横向溢出。
- 浏览器验证发现 Python `1.0` 经 JavaScript JSON 往返会变为 `1`，导致 preview Hash
  误报篡改；线协议改为整数置信度并加入 HTTP 回归。另修复配方控制台未进入主渲染。
- 对抗覆盖幂等账本损坏/悬空/事务失败、并发重放、伪造随机目录/Hash、旧/篡改编辑
  预览、备份路径/Hash/压缩炸弹/媒体类型/查询参数，以及全部攻击失败时当前库不变。
- 文档同步后的最终全量回归：Python `prototype/tests` 284 项（含 1 个可选依赖跳过）、
  Node 98/98、`scripts/tests` 16/16 通过。另行重跑全流程 3/3、HTTP+备份对抗 47/47、
  数据库+sampler+编辑对抗 112/112，全部通过。
- 浏览器和自动测试使用独立临时数据库并已清除。默认真实库保持 77,824 字节、mtime
  `2026-07-09T15:02:25.2769031Z`、SHA-256
  `88F597EF8094775F2449F8693806E99FA00FFD72EFC3782EF5CB273C03682395`。

### 未伪装为完成的边界

- 未提供真实 checkpoint、LoRA、代表性用户案例和出图结果，实际 1–2 轮成图质量与
  固定 Seed 稳定性尚不能验收。
- LoRA 一等资产、模型/LoRA 资料研究、多模型视觉共识融合、多人物角色卡、受管理图片
  资产、缩略图/检索和真正后端取消仍在后续阶段。
- 当前备份是现有数据库逻辑记录的安全迁移能力，不是包含配套图片的完整资产包。

## 2026-07-16 M0 数据基线、项目重开与词库编译底座

### 数据库基线

- SQLite 登记 schema v1 和 Prompt Studio `application_id`。空库直接初始化；兼容的
  旧 v0 库先做完整性与精确结构检查，再通过 SQLite backup API 生成人工故障回滚
  副本。该副本可能含密钥，只供迁移故障时由用户手动回滚，不可分享。
  未来版本、错误应用标识、残缺结构和随机字节文件均在写入前拒绝。
- 初始化改为服务启动时执行一次，数据库事务异常显式 rollback；并发初始化和并发
  版本写入均有隔离测试。
- 开发过程中只读检查默认数据库，未执行真实迁移；迁移前后基线校验使用临时库。

### 项目保存与重开

- 首页新增本机项目列表及加载、空、失败、正常四态；工作台新增作品名和保存状态。
  项目可重开全部持久化历史，以服务端版本为权威，从旧版本载入后保存为新的连续版本。
- 新增 `PUT /api/projects/<id>` 和版本 POST 的双重乐观并发检查。版本 POST 同时携带
  `baseVersion` 与 `baseUpdatedAt`，事务内先检查版本、再检查项目头；过期客户端收到
  结构化 409，不再有两个前端窗口静默覆盖版本或项目头的问题。PUT 同时强制携带
  `baseUpdatedAt`：缺失返回 `428 project_precondition_required`，过期返回
  `409 project_conflict`，名称、草稿和 metadata 也受 CAS 保护；版本 POST 返回
  `projectUpdatedAt` 供同一次保存的后续 PUT 使用。
- 保存、打开和列表请求增加去重/过期响应保护；所有会写回工作区的模型请求捕获会话
  ID、工作区修订和项目修订，同一项目中继续编辑后到达的旧结果也会被丢弃并清理加载
  状态。保存中不允许切换工作区，离开未保存内容前需确认，刷新/关闭浏览器也有
  `beforeunload` 原生确认；保存期间产生的新编辑不会被旧响应错误标记为已保存。
- 生成和本地应用修改只进入待保存状态，用户显式保存包含内容变化的作品后才新增
  服务端版本；页面按钮和当前说明已统一该语义。

### 词库编译与工程化

- 9 类、471 条随机元素新增严格编译器和可重建 v1 审查目录：稳定词条 ID、来源、
  抽取规则、目录 Hash 及 `--check` 已落地。v1 profile 只允许 printable ASCII
  `0x20–0x7E`，拒绝指定 Unicode 类别，限制单文件 128 KiB、单条 512 字节，并记录
  `normalizationUnicodeVersion = 15.0.0`。
- 当前目录为 `v1-05044da9a25cd532`，仍标记 `runtimeReady = false`，现有全随机尚未
  读取它；正式 runtime 发布前还需建立不可变目录版本历史。
- Windows 干净环境回归 workflow 已配置，覆盖 Python、Node、脚本测试以及 Unicode、
  文本提示词和随机词库生成产物一致性；首次线上 run 待验证。

### 本轮最终验证

- 按 README 的 AnimaDex 虚拟环境执行：Python 全量 160 项，其中 159 项通过、1 项因
  可选 NumPy 环境条件跳过；Node 前端 92/92 通过；脚本与生成器 16/16 通过。三组
  共执行 268 项，其中 267 项通过、1 项跳过；带 NumPy 的工作区运行时为 268/268。
- 真实浏览器复验完成“新建作品 → 保存草稿 → 首页项目列表 → 重开继续 → 再次
  新建”；修复了图片预览清理函数缺失导致的“新建作品”导航失效。
- 对抗式回归覆盖：双窗口版本/项目双 CAS、跨工作区及同工作区迟到异步结果、打开期间
  编辑、历史版本 metadata 与跨 text/image 恢复、未保存恢复确认、任意路径段
  `;extra` 路由别名（并保留编码分号 ID 与 query 分号）、v0 恶意 trigger/view、
  Unicode 隐形字符与超大词库输入。还修复了项目切换时识图 raw 泄漏、旧 loader
  卡死、旧 Tag 补全引用写入新项目、当前项目模式恢复错位和首次创建迟到响应覆盖新
  名称；修复后均通过。
- 自动测试未使用默认真实数据库。最终浏览器冒烟的临时环境变量继承异常曾让服务短暂
  对默认 v0 库登记 v1；进程随即停止，并以测试前记录的长度、修改时间和 SHA-256 为
  强校验，按实际仅变化的 SQLite 头字段原子恢复原始字节，随后清除误建回滚目录。
  最终默认库仍为 77,824 字节、原修改时间，SHA-256 为
  `88F597EF8094775F2449F8693806E99FA00FFD72EFC3782EF5CB273C03682395`，未丢失用户数据。
- 最终对抗复核未发现仍可复现的 P0/P1。已登记 1 个后续 P2：创建项目或追加版本在
  “数据库已提交、HTTP 响应丢失”时缺少幂等键/自动 GET 对账，当前不会静默覆盖数据，
  但用户需要重开项目确认，首次创建还可能留下孤儿/重复项目。

## 2026-07-14 对抗式边界测试与修复

- 发现 `SimpleHTTPRequestHandler` 会把 `prototype/data/prompt_studio.db`
  作为静态文件下载；该数据库可能含外部 API Key。静态服务现改为仅公开
  工作台所需的入口、JS、CSS 与 `assets/`，并拒绝编码路径绕过。
- 发现顶层数组、字符串或非 UTF-8 JSON 会造成部分 API 请求线程异常断开。
  现统一要求 UTF-8 JSON 对象并限制请求体为 30 MB，返回可读的 HTTP 400。
- 图片分析不再仅信任浏览器 MIME 声明，服务端会校验 PNG/JPG/WEBP 的文件签名，
  阻止伪装的任意字节进入本地视觉 worker。
- 新增 HTTP 边界回归测试，覆盖私有数据库访问、编码路径绕过、非对象/非 UTF-8
  JSON 与伪装图片数据。

## 2026-06-20

### 项目化

- 将仓库明确为“提示词工程 / Anima Prompt Studio”项目。
- 新增项目入口文档、架构说明、路线图和开发日志。
- 约定后续以 SQLite 作为 Prompt Studio 覆盖数据库。

### 已完成原型能力

- 首页包含“文生图”和“图片解析”入口。
- 文生图包含全随机、拓展提示词、参考提示词模式。
- 结构化编辑器支持锁定、单块再生成、应用成新版本。
- AnimaDex 本地资源已接入角色和画师搜索。
- 快速资源支持分页、搜索、预览、大图资源展示。
- 收藏已拆分为角色收藏和画师收藏。

### 下一步

- 新增 `prototype/db.py`。
- 新增项目、收藏、设置 API。
- 将收藏从 `localStorage` 优先迁移到后端 SQLite。

### 数据底座进展

- 已新增 `prototype/db.py`，初始化 `projects`、`prompt_versions`、`resources`、`favorites`、`settings` 表。
- 已新增 `/api/projects`、`/api/favorites`、`/api/settings` 等最小 API。
- 前端收藏已改为优先使用后端 SQLite，并保留 `localStorage` 作为离线兜底和旧收藏迁移来源。
- 设置已接入后端保存。
- “保存草稿”已开始写入项目库，并在已有作品上追加提示词版本。
- 本地数据库文件已创建：`prototype/data/prompt_studio.db`。

### 提示词模板编辑器

- 已新增 `prototype/prompts/text_expand.md`，作为文生图拓展的默认模型提示词模板。
- 已新增 `prototype/prompts/image_analyze.md`，作为图生图解析的默认模型提示词模板。
- 已新增 `/api/prompt-templates` 读写接口，模板 ID 使用白名单，避免路径越界。
- 文生图页面新增“编辑文生图模板”按钮。
- 图生图页面新增“编辑图生图模板”按钮。
- 模板编辑弹窗支持保存和撤回未保存内容。

### 本地提示词素材审计

- 只读扫描 `G:\新建文件夹`。
- 识别 1 份巨构规则、12 个 Anima 工作流、167 张样图和 8 个插件压缩包。
- 将提示词模板、模型质量词、LoRA 配方、ComfyUI 参数和插件能力分开归类。
- 新增 `docs/local-prompt-source-audit.md`，作为后续模板提炼和资源导入依据。

### 提示词框架原文归档

- 新增 `scripts/extract_prompt_frameworks.py`，从工作流、规则文本和插件 ZIP 中提取 LLM 提示词框架。
- 新增 `提示词原文归档`，包含去重阅读版、按来源完整副本和来源清单。
- 共归档 72 份不重复原文，对应 128 个来源实例。
- 其中普通框架 52 份，成人向或支持混合尺度的框架 20 份，已分目录放置。
- 原文未润色、未翻译、未纠错，并通过 SHA-256 逐文件校验。

### 基础文生图提示词 v1

- 将基础拓展规则拆为核心视觉扩写、混合输出模式、Anima 模型配置和双语 JSON 结构四个模块。
- 新增 `prototype/prompt_engine.py`，负责模块组合、OpenAI 兼容请求、模型 JSON 解析、格式修复重试和结构块规范化。
- 新增 `/api/text/expand`，支持本地 LLM 与外部 API 两种文本推理来源。
- 文生图“拓展并结构化”已移除模拟输出，模型不可用时显示真实错误。
- 设置抽屉新增本地/外部接口地址、模型名称和 API Key 配置。
- 提示词模板编辑器可以切换编辑四个基础文生图模块。
- 第一版默认目标为 Anima，使用无文字权重的 Tags 与简短自然语言混合格式。
- Anima 固定质量词与基础负面词改由后端强制注入；模型返回后会校验中英文成对，并从结构块重新编译总提示词。

### 拓展提示词正式母版

- 明确基础 v1 只是调用链骨架，不再将其视为原文整合完成。
- 新增 `docs/text-to-image-source-integration-map.md`，记录原文分流、能力提取和冲突裁决。
- 核心规则已加入题材路由、信息优先级、角色属性归属、左右肢体、多人互动、空间层次、构图、光影和逐项自检。
- 将拓展任务规则和 Tags/自然语言编译规则拆成独立模块。
- 增加 `strict`、`balanced`、`creative` 三档扩写强度，请求默认使用 `balanced`。
- 新增 `scripts/export_text_expand_prompt.py`，生成实际发送给 LLM 的完整系统提示词。
- 新增三组代表性审阅用例，覆盖短混合输入、中文人像和多角色互动。

### 本地 LLM 与外部 API 入口

- 选择 `llama.cpp CUDA 12.4` 作为第一版本地 OpenAI 兼容服务。
- 复用 RTX 4070 Ti SUPER 16GB 机器上已有的 `Huihui-Qwen3-14B-abliterated-v2.Q4_K_M.gguf`，模型 ID 使用文件名。
- 新增本地运行时安装、启动、停止和状态脚本，并让总启动器在模型已安装时自动启动 LLM。
- 新增 `/api/local-llm/status`、`/start`、`/stop` 和 `/api/text/provider-test` 接口。
- 设置抽屉新增本地模型启动、停止、连接测试，以及外部 API 连接测试。
- 外部 API 保留接口地址、模型名和 API Key 配置，后续取得服务凭据后可直接接入。
- 新增 `docs/local-llm-and-external-api.md`，记录目录、驻留策略、密钥边界和排错流程。

修正：确认桌面一键启动脚本已经使用同一套 llama.cpp 与 14B GGUF。已停止并删除重复下载，工作台改为复用 `H:\AI` 下的现有运行时和模型，不再维护项目内副本。

- 已用 `/api/text/provider-test` 验证现有模型连接，清理 Qwen3 的思考标签后返回 `OK`。
- 已用 `1girl, raining` 完成一次真实 `balanced` 拓展，成功返回十个中英双语结构块、正负提示词、关系描述和 assumptions。

### 文生图扩写质量深度修正

- 确认旧运行母版约占 5742 tokens，重复禁令过多，并以“透明伞、雨夜霓虹街道”作为具体示例，导致短输入被固定场景锚定。
- 将运行母版压缩为约 4000 字符，移除具体内容示例，改用明确的结构职责、密度目标、物理反应和低假设规则。
- `balanced` 对稀疏输入采用中性场景骨架，禁止无依据编造发色、瞳色、具体服装、地点和道具；丰富度转移到天气影响、空间、构图与光影。
- `creative` 改为两阶段：先生成受限视觉蓝图，再编译为中英双语十块结构，真实测试达到 46 个有效视觉项。
- 新增内容质量门槛：检查有效信息密度、场景/构图/光影职责、跨块重复、动作错放、关系描述和年龄层漂移；不再把“JSON 合法”视为质量合格。
- 质量门槛按输入信息量自适应，详细输入不再被统一逗号数量误判为过短。
- 本地 llama.cpp 改为 `--parallel 1`，避免四个并发槽分摊 8192 上下文而导致复杂请求溢出。
- 重写请求不再复制完整失败稿，避免第二次生成把上下文撑满。
- 真实验收覆盖：稀疏 balanced、详细中文 balanced、短输入 creative、多人严格模式。
- 文生图输入区新增“严格 / 均衡 / 创意”扩写强度选择，并随本地设置与生成请求保存；默认使用均衡。

### 桌面启动入口拆分

- 删除桌面的“一键启动 ComfyUI + 本地LLM”组合快捷方式。
- 新增 `启动 ComfyUI.cmd`：只启动 ComfyUI `8191`，不启动或重启本地 LLM。
- 新增 `启动 提示词工程.cmd`：启动 Prompt Studio、AnimaDex 和本地 LLM，并打开工作台。
- 实测 ComfyUI 独立启动前后 LLM PID 保持不变；提示词工程入口会复用已运行的 LLM，避免重复进程。
- 修复桌面 `.cmd` 直接包含中文路径时的 CMD/PowerShell 编码不稳定问题：改用纯 ASCII 文件内容与 PowerShell 编码命令，并由 CMD 直接打开目标 URL。
- 按桌面双击等价流程复测：两个脚本退出码均为 0，Prompt Studio 服务器收到首页请求，ComfyUI 与 Prompt Studio 均成功打开 Chrome 页面。

### 结构块真实随机变体

- 将结构编辑器原有的预设变体替换为 `/api/text/regenerate-block` 真实模型调用。
- 支持主体、外观、动作、场景、构图、光影和效果随机；质量、画师和负面词由模型配置或用户资源管理，不参与随机。
- 随机候选生成后增加第二次兼容性复核，防止目标块改写或冲突其他结构块；锁定块视为不可变事实。
- 结果先作为待应用修改，不立即覆盖已应用版本；当时点击应用会推进前端本地版本状态，
  2026-07-16 起改为显式保存后才创建服务端版本。
- DeepSeek 实测场景变体成功，本地 LLM 实测光影变体成功；两者均返回中英文对应内容。

### 完整提示词保真拆解

- 文生图“拓展”输入区新增独立的“仅拆解”按钮，原“拓展并结构化”按钮及 `/api/text/expand` 调用逻辑保持不变。
- 新增 `/api/text/decompose` 和独立拆解规则，不复用扩写提示词，也不读取严格、均衡、创意扩写强度。
- 模型逐项返回原始片段、分类及中英文对应内容；服务端按用户输入重新映射精确原文。
- 每个原始片段必须且只能出现一次。出现缺失、重复、改写或凭空新增时整次拒绝应用，输入框与原结构化版本保持不变。
- 无法可靠分类的内容进入“其他 / 未分类”；仅拆解不自动注入 Anima 质量词或负面词。
- 本地 LLM 使用混合输入 `1girl, long black hair, standing in rain, 红色围巾, cowboy shot` 实测成功，返回 11 个结构块，原文覆盖率 100%。
- 回归验证：Python 55 项测试、Node 28 项测试全部通过；1920x1080 页面检查确认“仅拆解”和“拓展并结构化”同时可见。

### 文生图结果渲染故障修复

- 真实浏览器复现发现：模型请求成功后，结构块渲染抛出 `RANDOM_VARIANT_BLOCKS is not defined`，导致“仅拆解”和“拓展并结构化”都停在结果显示之前。
- 根因是状态逻辑与浏览器启动逻辑位于两个独立闭包，随机变体配置只存在于前一个闭包；Node 状态测试没有执行浏览器闭包，因此此前未能发现。
- 将随机变体块 ID 通过 `PromptStudioApp` 显式传给浏览器启动层，并新增对应回归测试。
- 更新前端缓存版本，避免浏览器继续使用故障脚本。
- 实际页面复测：仅拆解成功生成 V1 和 11 个结构块；拓展成功生成 V1、Anima 质量词、双语正负提示词。新版本控制台无错误。

### 文生图任务进度框

- 在“仅拆解”和“拓展并结构化”按钮下方新增常驻进度框。
- 空闲状态提示等待任务；运行状态显示任务名称、模型来源、服务端生成与校验说明及实时耗时。
- 成功后保留完成状态和总耗时；失败后保留具体错误，不再只依赖短暂 Toast。
- 拆解和拓展共用同一套进度状态，但不修改各自的模型请求、提示词或结果结构。
- 实际页面验证外部 API 拆解成功，进度框显示“拆解完成 · 2.0 秒”，同时生成 V1 和结构化内容。

### 结构块联合随机

- 结构化编辑器为主体、外观、动作、场景、构图、光影和特效增加联合随机选择框；质量、画师、负面词和未分类内容不可选择。
- 编辑器顶部新增联合随机操作栏，显示已选数量，并提供清空和联合随机按钮；至少选择两个块才可运行。
- 新增 `/api/text/regenerate-blocks`：模型一次生成全部所选块，再进行一次跨块与固定块整体兼容性审校。
- 所选块必须全部返回且全部发生变化；遇到未变化或结构不完整时自动重试一次。
- 联合结果作为待应用编辑写入，不立即改变右侧已应用提示词；当时点击应用会推进前端
  本地版本状态，2026-07-16 起改为显式保存后才创建服务端版本。
- 真实页面验收选择场景、构图、光影：三块均变化，未选动作保持不变，右侧输出保持旧版本，待应用栏正确显示 3 个修改。

### 本地 LLM 长提示词拆解稳定性

- 用户长提示词拆解运行 71 秒后返回“模型输出不是严格 JSON”；模型日志确认请求未超时、未截断，而是生成约 3467 token 后 JSON 语法损坏。
- 拆解协议改为紧凑索引格式：模型只返回 `index`、分类和另一语言翻译，原始提示词由服务端按 index 精确恢复，不再要求模型重复抄写长原文。
- 拆解首次出现 JSON 无效、结构无效或原文覆盖失败时，后端自动进行一次完整修复重试。
- 保留旧版 `source/en/zh` 返回格式的兼容解析，已有外部 API 或测试数据不受影响。
- 使用截图中的 31 项长英文提示词真实调用本地 14B 模型，17.7 秒成功完成，原文覆盖率 100%，返回 11 个结构块。

### 本地 LLM 82 项提示词拆解修复

- 使用用户输入框中的完整 1633 字符、82 项提示词复现，确认此前 31 项样本通过并不能覆盖真实输入规模。
- 抓取两轮模型原始返回后定位根因：首轮长 JSON 随机生成重复逗号，修复轮又把右括号写到字符串外；两次均未超时或截断。
- 拆解改为每批最多 24 项，单批失败只重试当前批次，最后再统一执行 82 项索引覆盖和双语字段校验。
- JSON 解析器仅在字符串外清理本地模型偶发生成的 `U+0332` 组合下划线，不修改字符串内部提示词。
- 使用同一份 82 项输入在真实页面点击“仅拆解”，44 秒完成，接口返回 HTTP 200，结构化编辑生成 V1 和全部分类块。
- 回归验证：Python 64 项测试、Node 34 项测试全部通过。

### 右侧本地 LLM 再次翻译

- 在最右侧“中英文输出”标题区新增“本地 LLM 翻译”按钮，运行时显示“翻译中...”并禁用重复点击。
- 新增 `POST /api/text/translate-pending`，固定使用已保存的本地 LLM 配置，不受当前文生图任务选择的外部 API 影响。
- 只收集以“待本地 LLM 翻译：”开头的结构块；已有正常中文、英文原文、权重、锁定状态和来源保持不变。
- 模型必须完整返回全部项目 ID，且每项中文非空并包含中文字符；缺失、重复或无效结果整次拒绝应用。
- 翻译成功后同步更新当前结构块、已应用结构块、右侧输出和当前版本快照，但不增加作品版本号。
- 真实页面验证：随机作品 V1 带入 Hatsune Miku 后应用为 V2，右侧出现 2 段待翻译内容；点击按钮后接口 HTTP 200，两段均完成中文翻译，待翻译数量变为 0，版本保持 V2。
- 1920×1080 布局检查确认翻译按钮与目标模型选择框均位于右侧输出面板内且不重叠。
- 回归验证：Python 68 项测试、Node 37 项测试全部通过。

### 图片解析本地图片载入修复

- 修复图片选择后页面看起来没有反应的问题：旧实现只保存文件名，没有读取本地文件、保留 `File` 对象或生成预览。
- 新增 PNG、JPG、JPEG、WEBP 格式校验；不支持的文件会明确提示，不进入已载入状态。
- 使用浏览器本地对象 URL 显示原图预览，图片不会上传到外部服务；重新选图时会释放上一张图片的对象 URL。
- 上传区域在成功后显示文件名、文件大小和原图预览，并允许点击重新选择同一文件。
- “开始多模型分析”按钮在图片真正载入前保持禁用，成功载入后启用。
- 拖拽和文件选择器统一使用同一条载入逻辑，状态记录文件名、MIME 类型和大小，并保留原始 `File` 对象供后续本地识图模型使用。
- 使用本地 `1.webp` 真实验收：306 KB、2048×1556，预览正常显示，状态提示“图片已载入，可以开始分析”，分析按钮已启用。
- 回归验证：Node 40 项测试全部通过。

### 图片解析固定模拟结果移除与真实 Florence 接入

- 完整复现确认旧“开始多模型分析”没有发送图片：前端仅运行 850ms 定时器切换模型状态，结束后无条件载入 `data.js` 中固定的雨夜银发女性结果。
- 删除 `ADVANCE_ANALYSIS`、`startAnalysisSimulation`、固定 `mergedImageResult` 和硬编码原始合并结果，页面不再生成与上传图片无关的演示内容。
- 新增 `/api/vision/analyze`：浏览器把当前本地图片 Base64 发送到本机 Prompt Studio 服务，限制 PNG/JPG/WEBP 和 20 MB，不上传外部网站。
- 新增独立 `vision_worker.py`，复用本机已有的 ComfyUI Python 运行环境与 `Florence-2-base-PromptGen-v2.0` 模型，但不调用 ComfyUI 工作流；每次识图结束进程退出并释放模型内存。
- Florence 原始标签继续交给现有本地 LLM 做结构分类和双语对应；结构块来源统一标记为“Florence + 本地 LLM”。
- 新增中文完整性修复：纯英文中文栏会调用专用翻译器；首次照抄英文时自动纠错重试，并稳定归一化 `1girl`、`1boy`、`solo`、`multiple girls` 等常用标签。
- 当前只启用已真实安装的 Florence。WD14 未找到模型文件，JoyCaption/QwenVL 尚未接成独立服务，外部视觉 API 尚未配置，这些选项均禁用并明确显示“未安装”，不再伪造完成状态。
- 真实页面全流程验证绿色图书馆图片：识别出 `book`、`plant`、`library`、绿色连衣裙、眼镜和发髻，生成 V1、中文对应版本及 Florence 原始结果。
- 使用第二张多角色立绘对比：识别出 `multiple girls`、粉/绿/棕/金发和棕色连衣裙；两张图片原始结果与最终提示词均不同。
- 多角色图最终中文归一化包含“一名女性角色、单人、多名女性角色”，不再残留 `multiple girls`。
- 回归验证：Python 75 项测试、Node 42 项测试全部通过。
## 2026-06-22 本地多模型识图接入

- 将 WD14、JoyCaption 和 Qwen3-VL 从 ComfyUI 节点中解耦为 Prompt Studio 可直接启动的独立 worker。
- Florence 继续复用原有本地模型与 CPU worker。
- 新增统一分析器注册表、真实安装状态、固定顺序调度、部分失败保留和逐块中文翻译回退。
- WD14 使用 `SmilingWolf/wd-v1-4-convnextv2-tagger-v2`，ONNX CPU 推理，模型约 0.36GB。
- JoyCaption 网页运行采用 Beta One Q4_K GGUF 和专用 mmproj，合计约 5.40GB，通过本机 llama.cpp 在 CPU 运行；已有 Beta One FP8 9.31GB 模型仍保留并完成过独立 GPU 验证。
- Qwen3-VL 使用 `Qwen3-VL-4B-Instruct-GGUF` Q4_K_M 与 F16 mmproj，合计约 3.11GB，通过本机 llama.cpp 在 CPU 运行。
- CPU 方案避免与常驻的 14B 本地文本 LLM 争抢 16GB 显存，不会自动关闭本地 LLM 或 ComfyUI。
- 修复 Windows 下 llama.cpp 无法读取含中文绝对路径的问题：worker 向 llama-server 传递 ASCII 相对模型路径。
- 修复 mmproj 默认占用 GPU 的问题：JoyCaption 与 Qwen3-VL 使用 `--no-mmproj-offload`。
- 修复 JoyCaption FP8 在 Transformers 4.57.6 下快速图像处理器与 LANCZOS 不兼容的问题，独立 GPU 验证改用慢速图像处理器。

### 实图验证

- 测试图：`prototype/assets/green-library.png`
  - WD14：约 2.1 秒，识别到 `book, plant, bookshelf, glasses, library, hair bun`。
  - Florence：约 10 秒，识别到 `green dress, book, plant, library, reading`。
  - JoyCaption FP8 GPU：约 55.7 秒，生成图书馆、绿裙、书本和植物的自然语言描述。
  - JoyCaption GGUF CPU（本地 LLM 与 ComfyUI 同时运行）：约 16-21 秒。
- 测试图：`prototype/assets/rooftop-pilot.png`
  - WD14：约 2.1 秒，识别到 `helmet, red hair, cityscape, sunset, aircraft`。
  - Florence：约 10 秒，识别到 `jacket, gloves, sky, jet, helmet, skyscraper`。
  - JoyCaption FP8 GPU：约 56.1 秒，生成红发飞行员、屋顶城市和黄昏场景描述。
  - Qwen3-VL CPU（本地 LLM 与 ComfyUI 同时运行）：约 48-51 秒。
- 四模型 HTTP 组合测试：`green-library.png`，WD14、Florence、JoyCaption、Qwen3-VL 均为 `ready`，总耗时约 166.7 秒；随后由本地 14B LLM 输出结构化中英文结果。
- 验证结果：`prototype/data/vision-validation-green-library.json`。
- 浏览器状态截图：`prototype/multi-vision-installed.png`。

### 已知限制

- 四模型组合采用串行执行，当前机器约需 2-3 分钟。
- JoyCaption 与 Qwen3-VL 选择 CPU 是为了与常驻本地文本 LLM 共存；关闭本地 LLM 后可改为 GPU 加速，但当前网页不自动管理用户进程。
- 多模型描述存在信息重复，结构化 LLM 会分类整理，但不会删除所有语义近似项。
## 2026-06-22 模型驱动全随机

- 定位旧“全随机”问题：前端 `randomOutput()` 仅在两条静态短示例之间切换，完全不会调用本地 LLM 或外部 API。
- 删除静态随机成功路径，新增 `POST /api/text/random`。
- 新流程固定为两阶段：
  1. 当前选择的模型生成 45-60 项连贯视觉事实的随机画面蓝图。
  2. 使用现有 Anima 核心规则、混合提示词格式、模型配置和双语结构 schema 编译十个结构块。
- 全随机固定使用创意密度校验，至少要求 35 个有效视觉细节；结构或密度失败时修复重试一次。
- 前端新增全随机的开始、成功、失败状态，并将现有进度面板动态显示在全随机页面。
- 修正外观校验误判：允许“机械前臂、关节手部、腿甲”等可见身体外观，只继续禁止站立、行走、下蹲等动作/姿态词进入外观块。

### 外部 API 实测

- 保存配置：`deepseek-v4-flash`，`https://api.deepseek.com`。
- 直接 HTTP 测试：约 27 秒，10 个结构块，英文正向 3214 字符，中文正向 930 字符。
- 浏览器按钮测试：约 15 秒，10 个结构块，英文正向 1792 字符，中文正向 496 字符。
- 两次结果均为不同的完整原创场景，证明不再返回静态示例。
- HTTP 验证结果：`prototype/data/external-random-validation.json`。
- 浏览器结果截图：`prototype/external-random-generated.png`。

## 2026-07-09 仓库完整性修复与前端工作台升级

### 仓库完整性

- 发现 `.gitignore` 中非锚定的 `models/` 同时忽略了
  `prototype/prompts/text/models/`，导致核心提示词模块 `anima.md`
  从未入库，干净克隆后 87 个 Python 测试有 10 个失败。
- 修复：`models/` 改为 `/models/`；从编译产物
  `prompts/compiled/text_expand_anima.md` 逐字节还原 `anima.md`，
  `test_compiled_review_file_matches_runtime_system_prompt` 通过证明还原精确。

### 前端安全与健壮性

- 统一 6 处 innerHTML 渲染的 HTML 转义（结构块 textarea、分析器列表、
  队列指示、参考卡片、Raw 标签页、设置抽屉模型列表），阻断 LLM 输出
  或服务端错误文本破坏 DOM / 注入脚本的路径。
- `apiJson` 对非 JSON 错误响应容错，回退为可读中文提示。
- 文本拓展、全随机、拆解、图片分析接入 AbortController，
  进度面板与分析页提供取消按钮（前端中断等待；后端推理仍会完成）。
- 合并 `renderBlocks` 折叠/展开两套重复模板为 `renderBlockCard`。

### 编辑器能力

- 实时编译：块编辑与权重调整即时预览最终输出（`previewOutput`），
  "应用"语义不变仍固化版本；前端 `compileBlocks` 与后端对齐
  （当时按逗号/分号拆项 + lowercase 跨块去重；2026-07-16 已升级为统一规范化键）。
- 权重语法落地：非 100% 编译为 `(item:factor)`，0% 剔除该块；
  展开的结构块下方显示该块编译进英文正向的实际片段。
- 随机变体保留变体前内容，支持对比后"保留新变体/回退"；联合随机同。
- 版本时间线：版本徽标旁可点回滚（当时前端直接推进版本；2026-07-16 起改为载入
  待保存内容，显式保存后才由服务端生成新版本，历史不丢）。
- 快捷键：Ctrl+Enter 拓展、Ctrl+Shift+Enter 应用、Ctrl+S 保存、Esc 关闭弹层。

### 新功能

- 画师混合器：画师收藏页勾选画师并配权重（10%-150%），实时预览
  `(tag:factor)` 组合并一键写入画师块。
- 配方：编辑头部"存为配方"把十个结构块整组存为 `type=snippets`
  且带 `blocks` 的收藏（复用 favorites API，无后端改动），
  素材库带入即整组回填。
- 变体矩阵：并行发起 3 次联合随机，候选卡片网格对比后择一应用，
  部分失败保留其余候选。
- Tag 联想补全：英文结构块输入 ≥2 字符弹出联想（前缀优先），
  ↑↓/Enter/Tab/Esc 操作；词典为 `data.js` 的 `promptTagDictionary`
  （224 项）并合并当前块内已有条目。

### 测试

- Node 测试 44 → 60（转义回归、取消、实时预览、权重编译、
  变体对比、版本回滚、配方、矩阵、Tag 建议排序等）。
- Python 87 个测试在干净克隆下全部通过。

## 2026-07-16 GitHub 整合与本地边界收口

### GitHub 差异与安全整合

- 获取并逐文件比较 `origin/main` 的 3 个新提交：产品需求与实施文档、工作台升级、
  以及九类共 471 条词库原稿。
- 将原工作区完整保存到 `codex/pre-sync-snapshot-20260716`（`65a52a8`），再从
  `origin/main` 创建 `codex/github-sync-20260716`，只回放确认过的本地真实增量。
- 整合提交 `06a8dd2` 保留远端 PRD、路线图、词库和前端升级，同时带回本地服务器、
  测试和文档加固；整个过程未使用破坏性 reset，也未推送 GitHub。

### HTTP、密钥和模型副作用边界

- Host 仅允许回环和显式配置值；非回环监听需要双重显式开关，防止 DNS rebinding
  和误暴露局域网服务。
- 写接口校验同源 `Origin`/`Sec-Fetch-Site` 与 UTF-8 JSON；GET/HEAD 共用公共静态
  白名单，响应补齐 CSP、nosniff、frame deny 与 Referrer-Policy。
- 严格校验唯一非负 Content-Length，拒绝 Transfer-Encoding；请求头同时设置空闲和
  固定总时限，请求体使用固定总 deadline，拒绝短 body、慢速滴流、重复 JSON key、
  非有限数和非对象顶层。视觉大图使用独立 30 秒正文时限，避免合法 30 MiB 请求被
  普通 JSON 的 2 秒时限误拒。
- LLM start/stop 必须先完整读取并验证空 JSON 对象，再执行进程副作用；异常或不完整
  body 不会启动/停止模型。
- 设置 GET 不再回显 API Key，只返回 `*Configured`；PUT 省略、空白或掩码值会保留
  已有密钥。项目/版本/收藏 metadata 递归过滤 token、secret、password、private key、
  credential 等别名。
- 图片分析在字节和像素上限后由 Pillow 完整 verify/load；AnimaDex 缩略图兼容 URL
  编码的斜杠 slug，同时拒绝控制字符和路径穿越段。

### 数据与前端一致性

- 新建项目/版本/收藏采用类型、长度、枚举和严格 UTF-8 schema；版本号在
  `BEGIN IMMEDIATE` 事务中由服务端连续分配，错误稳定返回 400/409/500 JSON。
- 历史项目中含空格、Unicode 或斜杠的 ID 仍可读取和追加版本；历史坏 JSON/非有限
  block 降级为安全默认，复杂 AnimaDex 收藏 ID 使用稳定 hash 路由，删除会清理重复行。
- 结构块权重在导入、配方、版本恢复、图片结果和数据库入口统一为 0–120；画师混合器
  独立保留 10–150。
- 前后端统一以
  `Unicode 15 已分配片段 NFKC → 固定 Unicode 15 单码点 lowercase 表 → Unicode
  White_Space/BOM 折叠 → ß→ss` 生成去重键；Unicode 15 未分配字符原样保留。
  分配范围和 1,433 条 lowercase 映射由脚本生成给 Python 与 JS，防止 Python Unicode
  15 与浏览器/Node Unicode 17 的版本漂移。U+FEFF、U+0085、U+200B、U+001C、
  U+1C89、U+A7F1 与 Greek Final Sigma 上下文均有跨语言测试，首个原始文本仍保留展示。
  未知状态降级为 `unknown`，项目 metadata 不再混入设置密钥。
- 收藏快速添加/取消按资源 ID 串行化，保留服务端 `favoriteId`，消除竞态和错误删除。

### 验证

- Python `prototype/tests`：131 项，130 通过，1 项因缺少可选 NumPy 跳过。
- Node：74/74；`scripts/tests`：4/4；`py_compile`、`pip check`、`git diff --check`
  全部通过。
- 最终跨运行时穷举覆盖全部 1,112,064 个 Unicode scalar 的 4 组常规场景，以及
  286,719 个 Unicode 15 已分配 scalar 的 4 组 Greek Final Sigma 上下文场景；共
  5,595,132 组 Python/JS 成对结果，差异为 0。
- 所有 HTTP/数据库攻击用例使用真实本地测试服务器、临时 SQLite、假密钥与模型 stub；
  不调用真实模型或外部 API。最终对抗矩阵记录在
  `docs/github-sync-audit-2026-07-16.md`。
- 主数据库保持 77,824 字节，SHA-256 为
  `88F597EF8094775F2449F8693806E99FA00FFD72EFC3782EF5CB273C03682395`，测试前后未变。
