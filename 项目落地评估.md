# 提示词工程总文档：项目落地评估

## 一、结论

这份文档最有价值的不是具体 tag 或采样参数，而是它提出的分层思路：

1. 用户用中文描述需求。
2. LLM 负责理解意图、补全场景和输出结构化数据。
3. 确定性代码负责将结构化数据转换成不同模型的提示词。
4. 模型配置、负面提示词和变体策略独立管理。

这套结构适合作为项目的第一版架构，但不能直接照搬代码。当前文档更接近“方案草稿 + 规则样例”，距离稳定产品还缺少数据协议、校验、测试、版本管理和安全边界。

## 二、对项目最有帮助的内容

### 1. 建立统一的中间场景协议

文档中的 `scene_json` 是整个方案的核心。它让上游场景理解与下游模型格式解耦：

- 更换 LLM 时，不必重写所有模型转换器。
- 新增绘图模型时，只需增加一个适配器。
- 同一场景可以输出多种模型格式。
- 可以保存、编辑、复用和比较场景数据。
- 测试时可以绕过 LLM，直接用固定 JSON 验证转换结果。

建议保留这个思路，但升级为带版本号的正式 Schema，例如：

```json
{
  "schema_version": "1.0",
  "subject": {},
  "appearance": {},
  "environment": {},
  "composition": {},
  "lighting": {},
  "style": {},
  "generation": {}
}
```

### 2. LLM 与规则代码分工合理

文档把任务分为：

- LLM：语义理解、场景补全、逻辑推理。
- Python：格式转换、固定规则、预设选择。

这个边界是对的。能确定的内容应尽量由代码完成，避免每次生成都产生不可预测的格式差异，也能降低调用成本。

### 3. 多模型适配器思路可扩展

`to_illustrious`、`to_pony_v6`、`to_anima` 等函数已经体现了适配器模式。项目中可进一步统一成接口：

```python
class PromptAdapter:
    def render_positive(self, scene, profile) -> str: ...
    def render_negative(self, scene, profile) -> str: ...
    def validate(self, scene, profile) -> list[str]: ...
```

这样可以把模型差异、参数建议和校验规则收拢在各自适配器中。

### 4. 配置与代码分离的方向正确

规则表和负面提示词预设适合放入配置文件，而不是散落在业务代码中。后续可以支持：

- 按基础模型和 checkpoint 版本选择配置。
- 用户自定义预设。
- 配置热更新。
- A/B 测试不同规则。
- 回滚到旧版本。

### 5. 批量变体有明确产品价值

从同一场景生成构图、角度、表情或光线变体，适合做成项目中的独立能力。但变体应当可复现、可控，而不是直接修改原始对象并依赖全局随机数。

## 三、不能直接照搬的部分

### 1. 中间 JSON 混合了自然语言与 tag

当前字段有时要求英文描述，有时又期待 `full_body`、`from_below` 等 tag。转换器只是用逗号拼接，无法真正把自然语言转换成模型需要的规范 tag。

例如：

```json
{
  "hair": "long blue hair flowing in the rain"
}
```

直接拼入 Danbooru 风格提示词，并不等价于：

```text
long_hair, blue_hair, wet_hair
```

建议把数据拆成“规范枚举/tag”和“自然语言描述”两层：

```json
{
  "hair": {
    "tags": ["long_hair", "blue_hair", "wet_hair"],
    "description": "long blue hair soaked by rain"
  }
}
```

### 2. Schema 信息不足

当前协议缺少很多下游真正需要的信息：

- 角色、作品、画师是混在一个字符串里的。
- 没有模型 profile、checkpoint 版本和 LoRA 信息。
- 没有宽高比、目标分辨率、种子和采样参数。
- 没有字段置信度和用户明确指定/系统推导的来源标记。
- 多角色时无法描述每个角色的独立外观、动作和空间关系。
- 没有冲突约束，例如“夜晚”和“正午阳光”同时出现。

建议将 `subject` 改为数组，并将身份、外观和动作绑定到具体角色。

### 3. 转换器只是字符串拼接器

现有代码缺少：

- tag 规范化。
- 空值和重复值清理。
- 权重语法。
- 冲突检测。
- 模型不支持字段的降级策略。
- 提示词长度预算。
- 不同 checkpoint 的方言差异。

建议建立标准化流水线：

```text
Scene Schema
  -> Normalize
  -> Validate
  -> Resolve conflicts
  -> Select model profile
  -> Render
  -> Post-process
```

### 4. 随机变体会污染原始数据

示例中的 `generate_variants` 在循环中直接修改 `scene_json`，调用结束后原始场景已被改变；同时没有随机种子，结果无法复现。

应当：

- 每次使用深拷贝。
- 接受 `seed` 参数。
- 记录每个变体修改了哪些字段。
- 对互斥选项做约束采样。

### 5. 规则匹配容易误判

`trigger in prompt_text` 是子串匹配，会产生误命中。例如短词可能出现在另一个单词中；自然语言、下划线 tag 和连字符也没有统一处理。

建议先将输入规范化为 token，再按精确 tag、同义词和字段路径匹配。规则最好直接作用于结构化场景，而不是作用于最终字符串。

### 6. 模型知识被硬编码为永久事实

模型格式、质量 tag、采样参数和负面提示词通常与具体模型版本、checkpoint、工作流甚至前端实现有关，不能只按模型家族写死。

建议使用版本化 profile：

```yaml
id: illustrious-xl/example-checkpoint@1
prompt_style: danbooru
supports_negative_prompt: true
defaults:
  steps: 24
  cfg: 6
quality_tags:
  - masterpiece
  - best_quality
```

所有参数应标注来源、验证日期和适用版本，并允许通过实测覆盖默认值。

### 7. LLM 输出不能直接 `json.loads`

生产环境中可能遇到：

- 非法 JSON。
- 字段缺失。
- 枚举值错误。
- 多余解释文字。
- 内容与用户要求冲突。

需要使用 JSON Schema 或 Pydantic 校验，并增加有限次数的修复重试。若所用模型/API支持结构化输出，应优先启用原生结构化输出。

### 8. 内容转换模块需要独立治理

文档中的成人内容转换规则不适合与通用场景理解、模型适配代码混在一起。无论项目最终是否保留这类能力，都应当作为独立、默认关闭的可选模块，并具备：

- 明确的年龄与内容边界。
- 对未成年人、年龄不明角色和受保护情形的硬性阻断。
- 权限控制和审计。
- 单独的测试、配置和发布开关。
- 不让内容等级转换破坏原始场景语义。

从项目建设顺序看，第一阶段应先完成纯 SFW 主链路。

## 四、建议的项目架构

```text
src/
  domain/
    scene_schema.py
    enums.py
  reasoning/
    scene_expander.py
    prompts/
  normalization/
    tag_normalizer.py
    conflict_resolver.py
  adapters/
    base.py
    illustrious.py
    pony_v6.py
    pony_v7.py
    anima.py
    zimage.py
  profiles/
    loader.py
  pipeline/
    generate.py
    variants.py
  safety/
    policy.py
  api/
    schemas.py
    routes.py
profiles/
tests/
  fixtures/
  test_schema.py
  test_adapters.py
  test_variants.py
```

核心对象建议分为：

- `SceneSpec`：与模型无关的场景事实。
- `ModelProfile`：具体模型/checkpoint 的能力和默认参数。
- `PromptResult`：正向提示词、负向提示词、参数、警告和追踪信息。
- `VariantSpec`：变体轴、随机种子和修改记录。

## 五、建议的实施顺序

### P0：先做可验证的最小闭环

1. 定义 `SceneSpec v1` 和枚举。
2. 实现结构化输出校验与重试。
3. 只支持一个最常用目标模型。
4. 建立 20-50 个固定场景样例。
5. 为适配器增加快照测试。
6. 输出正向提示词、负向提示词、生成参数和警告。

### P1：扩展多模型

1. 引入版本化 `ModelProfile`。
2. 增加其他模型适配器。
3. 加入 tag 词典、同义词和冲突规则。
4. 支持用户覆盖 profile 默认值。
5. 建立不同模型的效果评测集。

### P2：变体和产品化

1. 实现不修改原场景的确定性变体。
2. 支持构图、镜头、光线、服装和环境等变体轴。
3. 保存输入、场景 JSON、最终提示词、profile 版本和 seed。
4. 加入对比生成与用户评分，用数据迭代规则。

## 六、第一版验收标准

- 同一个 `scene_json + profile + seed` 必须得到完全一致的输出。
- 所有 LLM 输出都经过 Schema 校验。
- 空字段不会产生残缺句子或连续逗号。
- 多角色信息不会串到错误角色上。
- 适配器之间不共享模型专属 tag。
- profile 能明确追踪到版本和更新时间。
- 至少覆盖雨天、室内、夜景、多角色、近景、全身等典型场景。
- 不支持或相互冲突的要求会返回警告，而不是静默生成。

## 七、对原文档的定位建议

建议把原文档拆成三类资料：

1. **架构设计文档**：保留统一场景协议、分层流水线和适配器设计。
2. **模型 Profile 手册**：每个模型单独维护格式、参数、来源和实测记录。
3. **实验规则库**：tag、负面词、变体和可选内容规则作为可测试配置管理。

原文档可以继续作为需求与实验素材，但不应直接作为生产代码规范。
